# Serveur de clonage vocal local Callpme (Coqui XTTS-v2)
#
# ⚠️ PYTHON 3.11 (ou 3.10) OBLIGATOIRE — XTTS ne s'installe PAS sur 3.12 / 3.13.
#    Installe-le :  winget install Python.Python.3.11
#    (run.bat le détecte et l'utilise automatiquement.)
#
# coqui-tts = fork MAINTENU de Coqui TTS (même API « from TTS.api import TTS »,
# même modèle xtts_v2), plus fiable à installer aujourd'hui que l'ancien « TTS ».
# Si jamais ça coince, remplace la ligne par :  TTS==0.22.0
coqui-tts
fastapi
uvicorn[standard]
pydantic
# torch est tiré automatiquement. Pour le GPU NVIDIA (fortement recommandé),
# installe-le AVANT, via :
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
